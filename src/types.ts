export default interface groupDetails {
    groupName: string
    numParticipants: number,
    totalSeats: number,
    location: string,
    time: string,
    course: string,
    participantDetails: string[],
    details: string
}
  